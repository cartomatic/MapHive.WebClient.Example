//Disable some of the JSLint warnings
/*global window,console,Ext*/
(function(){
    //Make sure strict mode is on
    'use strict'
    
    Ext.define('ModernDesktopTest.view.map.MapModel', {
        extend: 'Ext.app.ViewModel',
        alias: 'viewmodel.map',
    
        stores: {
        },
    
        data: {
        }
    });
}());
